{
  "name": "@rockite/platform-adapter",
  "version": "0.1.1",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "unbuild",
    "prepack": "unbuild",
    "clean": "rimraf dist"
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "unbuild": "^3.0.1",
    "rimraf": "^6.0.1",
    "@rockite/typescript-config": "workspace:*"
  }
}
